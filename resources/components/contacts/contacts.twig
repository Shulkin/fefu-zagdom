{% import _self as macros %}

{% set scope = {
    phone: '+7 (900) 900-90-90',
    email: 'info@gmail.com',
    location: 'г. Владивосток<br>ул. Выселковая 49, стр. 3'
} %}

{% macro content(scope) %}
    <div class="ContactsSection">
        <div>
            <a href="tel:{{ scope.phone }}" class="ContactsItem">
                <img
                    src="{{ 'icons/phone-white.svg'|asset }}"
                    alt="" width="24" height="14"
                    class="ContactsItem__icon"
                >
                {{ scope.phone }}
            </a>
        </div>

        <div>
            <a href="email:{{ scope.email }}" class="ContactsItem">
                <img
                    src="{{ 'icons/email-white.svg'|asset }}"
                    alt="" width="24" height="14"
                    class="ContactsItem__icon"
                >
                {{ scope.email }}
            </a>
        </div>

        <div>
            <span class="ContactsItem">
                <img
                    src="{{ 'icons/location-marker-white.svg'|asset }}"
                    alt="" width="24" height="14"
                    class="ContactsItem__icon"
                >
                {{ scope.location|raw }}
            </span>
        </div>
    </div>
{% endmacro %}

{{ macros.content(scope) }}
